# 企业内部知识库工单智能体 — 项目大纲

## 1. 项目定位
基于 RAG 的企业内部制度知识库 + 工单智能体。核心特征：
- **本地化**：Embedding / 向量库 / OCR 全部本地运行，无云端依赖成本
- **演示性**：Streamlit 前端 + SSE 流式事件，可现场演示完整问答与工具调用
- **测试差异点**：独立压测套件 + Loop 闭环自优化，提供量化指标
- **LLM 生成层**：走云端 API（当前 Zhipu GLM-5.2，Volcano Ark 托管），key 走环境变量

## 2. 技术栈
- **后端框架**：FastAPI（SSE 流式响应）+ LangGraph（状态机编排）
- **检索链路**：单 Chroma 向量库 + 本地 BGE Embedding（`bge-base-zh-v1.5`，768 维）+ BGE-Reranker（占位 TODO）+ BM25 关键词兜底 + 阈值过滤
- **持久化**：SQLite（`app.db`），替代 JSON 解决并发读写
- **文档解析**：PDF + `.docx`（表格结构化 + 插图 PaddleOCR 本地抽取）
- **前端**：Streamlit（登录 / 对话 / 工单 / 上传 四页）
- **测试**：Pytest + Locust + Allure
- **运行环境**：Python 3.12.9 虚拟环境（系统 3.14 不兼容 paddlepaddle）

## 3. 架构分层（三层解耦）
- **主服务层** `agent_server/`：API 路由 + Graph Flow 编排 + RAG 检索 + 业务工具 + 核心组件
- **前端层** `web/`：Streamlit 对话界面，调用 SSE 接口，映射工具事件为中文提示
- **测试闭环层** `harness_test/`：压测 + 样本分析 + Loop 半自动 Prompt 自优化（产出 `prompt_diff.md`，人审）

## 4. 目录结构
```
knowledge_agent/
├── datas/                    公司制度文档（PDF / .docx）+ chroma/ + app.db
├── models/bge-base-zh-v1.5/  本地 Embedding 模型权重
├── agent_server/
│   ├── core/        auth, rbac, db, config, llm_client
│   ├── rag/         loader, chunker, embed_loader, vector_store, reranker, retriever_pipe, smoke_test
│   ├── tools/       schemas, business_tools
│   ├── graph_flow/  state, prompt_template, graph_nodes, graph_builder
│   ├── api/         auth, chat, ticket, knowledge, tool, utils
│   ├── main.py, smoke_test.py, logs/
├── web/             app.py, smoke_test.py
├── harness_test/    压测 / 闭环样本分析
├── docs/            演示截图与说明
├── requirements.txt
├── .env / .env.example / .gitignore
├── dev_plan.md / codex_prompts.md
```

## 5. 里程碑（M0–M6）
- **M0 项目骨架**：目录结构 + 配置（config.py 惰性加载 LLM key）+ 虚拟环境就绪 + `.gitignore` + `requirements.txt` + 基础冒烟（4 pytest + `/health` + `/docs`）
- **M1 RAG 链路**：loader（PDF/`.docx` + 表格结构化 + 插图 PaddleOCR）→ chunker → 本地 BGE embedding（768 维，禁云端）→ 单 Chroma → 可选 reranker（本地权重探测，无则占位 TODO）→ 关键词兜底 → 阈值过滤；交付 `rag/smoke_test.py`
- **M2 后端与 LLM**：FastAPI 路由（auth/chat/ticket/knowledge/tool）+ 注册登录鉴权 + RBAC（员工/管理员）+ LangGraph 编排节点（身份校验/文档检索/工单匹配/LLM 决策）+ SSE 流式工具事件 + LLM 真连自检；key 经 `core.llm_client` 中转，严禁硬编码
- **M3 前端演示**：Streamlit 单入口 `web/app.py`（sidebar 切 4 页）+ 真实链路对话（含工具调用过程可视化）+ `web/smoke_test.py` + 4 张交付截图
- **M4 压测与测试**：`harness_test/` 隔离环境 + Pytest 单测 + Locust 并发压测（50/100 并发 QPS / P95 / 失败率）；**强制 mock LLM**（`core.llm_client` monkeypatch）防烧 token
- **M5 Loop 闭环自优化**：读取 `agent_server/logs` 样本 → 规则护栏打分（幻觉/越权/无关）→ 半自动产出 `prompt_diff.md` + 优化建议，人审后生效；**LLM 评估默认关闭**（`LLM_EVAL_ENABLED`）
- **M6 交付文档**：`README.md`（全栈本地零 API 成本，仅 LLM 走云端，key 走 env）+ 简历落点说明 + 演示脚本

## 6. 共用约束
单 Chroma / SQLite / Streamlit / Reranker 占位 / Loop 半自动 / 规则护栏 / 本地模型路径固定 / 每里程碑自检可运行 / 依赖已预装禁 pip install / 固定 Python 3.12.9 / LLM 仅走 env 配置
