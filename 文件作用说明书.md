# Knowledge Agent 文件作用说明书

> 项目路径：`F:\code\knowledge_agent`
> 运行环境：Python 3.12.9（项目虚拟环境 `.venv`）
> 文档定位：本文件是一份**文件地图**，逐文件说明每个文件 / 目录的职责，便于二次开发与维护。

## 本文档与其他文档的关系

| 文档 | 侧重 | 读者 |
| --- | --- | --- |
| `README.md` | 全栈总说明：设计边界、本地启动、API 概览、主要函数映射、自检命令、量化结果 | 开发者 / 验收 |
| `项目大纲.md` | 架构蓝图、技术栈、三层解耦、M0–M6 里程碑与共用约束 | 规划 / 概览 |
| `使用说明书.md` | 终端用户操作：安装、启动、登录、上传、对话、工单、FAQ | 使用者 |
| `docs/*.md` | 架构图 / API 字典 / 演示指南 / 简历量化点 | 专项参考 |
| **`文件作用说明书.md`（本文件）** | **每个文件 / 目录是干什么的** | 维护 / 二次开发 |

---

## 一、顶层文件一览

| 文件 | 作用 |
| --- | --- |
| `README.md` | 全栈总说明（设计原理 + 函数映射 + 指标）。详见上表。 |
| `项目大纲.md` | 架构与里程碑总览（M0–M6）。 |
| `使用说明书.md` | 面向使用者的操作手册。 |
| `requirements.txt` | 合并依赖清单（后端 + 前端 + 测试），等价于 M0+M3+M4 之和。 |
| `.env.example` | LLM 配置模板：复制为 `.env` 后填写 `AGNES_API_KEY/ARK_API_KEY`、`AGNES_BASE_URL/MODEL`、`LLM_EVAL_ENABLED`。 |
| `.env` | 真实密钥文件（被 `.gitignore` 忽略，不入库）。代码 / 文档不写真实 key。 |
| `.gitignore` | 忽略规则：`.venv/`、`datas/`、`models/`、`__pycache__/`、`.env`、`web/*.log`、`agent_server/logs/`、`harness_test/results/*.csv` 等，防密钥与大数据入库。 |
| `run_harness.py` | 一键测试编排：跑 `harness_test/func`+`edge` 的 pytest（Allure）→ 临时端口起 uvicorn → 按 50/100 并发跑 Locust 短压测并解析 CSV → 写 `m4_summary.json`。 |
| `start_local_launcher.bat` | Windows 双击启动脚本：进入项目根并调用 `.venv\Scripts\python.exe web\local_launcher.py`。 |
| `start_local_launcher` 相关启动入口 | 见 `web/local_launcher.py` 与上方 `.bat`。 |

---

## 二、`agent_server/` —— 后端主服务

### 2.1 顶层

| 文件 | 作用 |
| --- | --- |
| `agent_server/main.py` | FastAPI 入口：挂载 5 个 router（auth/chat/ticket/knowledge/tool）与 2 个中间件（异常包装、限流），暴露 `GET /health`，`startup()` 初始化 SQLite 连接池。被 `uvicorn agent_server.main:app` 启动。 |
| `agent_server/smoke_test.py` | M2 端到端冒烟：用 TestClient 走「注册员工 → 登录 → 未授权拦截 → 对话建单 → 员工导出 403 → 管理员导出 / 知识库列表」全流程。 |
| `agent_server/logs/` | 运行时目录，存放 `qa_events.jsonl`（结构化问答日志），由 `core/qa_logger.py` 写入，是 M5 `loop_optimizer` 的输入源。 |

### 2.2 `core/` —— 核心组件

| 文件 | 作用 |
| --- | --- |
| `core/config.py` | LLM 配置读取：`get_llm_settings()` 从 env 取 AGNES/ARK 的 key、base_url、model、超时、温度，返回冻结 `LLMSettings`。被 `core/llm_client.py` 调用。 |
| `core/auth.py` | 认证与密码安全：`hash_password()`/`verify_password()`（PBKDF2-SHA256，260k 迭代）、`register_user()`/`login_user()`/`get_current_user()`（解析 `Bearer` token）、`change_password()`/`admin_reset_password()`/`admin_delete_user()`/`admin_create_user()`。 |
| `core/rbac.py` | 角色-工具权限表：`ADMIN_TOOL_NAMES`/`EMPLOYEE_TOOL_NAMES`、`role_tier()`、`available_tools()`、`ensure_tool_allowed()`（越权抛 403，未知工具 404）。 |
| `core/db.py` | SQLite 持久层：`SQLitePool`（连接池 + WAL + schema 初始化 user/ticket/chat_history/doc 四表）及全部 CRUD。被后端几乎所有模块调用。 |
| `core/llm_client.py` | 云端 LLM 客户端：`chat_completion()`/`stream_chat_completion()`；`mock_llm_enabled()` 支持 `KNOWLEDGE_AGENT_MOCK_LLM=1` 返回假数据；`get_client()` 关闭系统代理（`trust_env=False`）避免 Ark/Agnes HTTPS 异常。 |
| `core/qa_logger.py` | 结构化问答日志：把问答结果写成 `agent_server/logs/qa_events.jsonl`（含 question/answer/retrieval/guardrail/tool_events）。被 `chat_router` 调用。 |
| `core/__init__.py` | 包标记。 |

### 2.3 `rag/` —— 检索链路

| 文件 | 作用 |
| --- | --- |
| `rag/loader.py` | 文档解析与 OCR：`scan_source_files()`（忽略 `chroma`/`app.db`）、`load_documents()`/`load_pdf()`/`load_docx()`（表格转 Markdown、`.doc` 走 soffice 兜底）、插图 `PaddleOCR` 文本抽取 + `enhance_image_text()` 接本地 `qwen2.5-vl` VLM 语义增强、`vlm_status()`。 |
| `rag/chunker.py` | 块切分：`chunk_blocks()` 按窗口(500)/重叠(50)切分，表格与插图整块保留，生成带 hash id 的 `DocumentChunk`。 |
| `rag/embed_loader.py` | 本地 BGE Embedding：`get_embedding_model()`（固定 `models/bge-base-zh-v1.5`、维度强校验 768）、`embed_texts()`/`embed_query()`。 |
| `rag/vector_store.py` | 单 Chroma 封装：`RagVectorStore` 类，`upsert_chunks()`/`query()`/`rebuild()`/`update_document()`；失败抛 `VectorStoreUnavailable` 并保留关键词兜底。写 `datas/chroma/`。 |
| `rag/reranker.py` | 本地 reranker：`reranker_available()`/`reranker_enabled()`（默认关，需 `RERANKER_ENABLED=true`）、`rerank()`（用 `bge-reranker-base` CrossEncoder 重排）。 |
| `rag/retriever_pipe.py` | 检索总装：`rebuild_index()`（加载→切分→入库→清缓存）、`retrieve()`（向量 + BM25 兜底 + 阈值过滤 + 可选重排）、`keyword_search()`、阈值/分词/停用词规则。 |
| `rag/smoke_test.py` | M1 RAG 冒烟：加载并重建索引、打印 3 个代表问题 Top-3、验证低相关问题走兜底、`reranker_top1` 探针。 |
| `rag/__init__.py` | 包标记。 |

### 2.4 `tools/` —— 业务工具

| 文件 | 作用 |
| --- | --- |
| `tools/schemas.py` | 工具入参模型：`DocRetrieveInput`/`MatchSimilarTicketInput`/`CreateConsultTicketInput`/`QueryTicketListInput`/`ExportTicketStatInput`/`KnowledgeManageInput` + 公共 `validate_user_text()`（空白/长度/可见字符校验）。 |
| `tools/business_tools.py` | 6 个工具实现 + 注册表：`doc_retrieve`/`match_similar_ticket`/`create_consult_ticket`/`query_ticket_list`/`export_ticket_stat`/`knowledge_manage`；`TOOL_REGISTRY` 字典与 `call_tool()` 分发。 |
| `tools/__init__.py` | 包标记。 |

### 2.5 `graph_flow/` —— LangGraph 编排

| 文件 | 作用 |
| --- | --- |
| `graph_flow/state.py` | `AgentState` 数据类：贯穿所有节点的状态（user/question/rag_results/llm_answer/ticket_suggestion/guardrail/tool_events 等）。 |
| `graph_flow/prompt_template.py` | `SYSTEM_PROMPT`（要求模型只基于检索内容、输出指定 JSON）与 `build_decision_messages()` 构造 LLM 决策提示词。 |
| `graph_flow/graph_nodes.py` | LangGraph 节点函数：`identity_check_node()`/`parallel_rag_node()`（并行检索+工单匹配）/`llm_decision_node()`/`build_agent_context()`（合并最近 5 轮历史）/`guardrail_check()`（金额/条款/工单号幻觉检查）/`output_node()`/`run_agent()`/`run_agent_events()`（SSE）。 |
| `graph_flow/graph_builder.py` | 用 `StateGraph` 组装 `identity→rag→llm`（rag 最多重试 3 轮）状态图；`build_graph()`/`run_agent()`/`run_agent_events()`。被 `chat_router` 调用。 |
| `graph_flow/__init__.py` | 包标记。 |

### 2.6 `api/` —— 路由层

| 文件 | 作用 |
| --- | --- |
| `api/utils.py` | 通用件：统一成功响应 `ok()`、`ChatRequest` 模型、限流中间件 `rate_limit_middleware()`（可用 `KNOWLEDGE_AGENT_DISABLE_RATE_LIMIT=1` 关闭）、异常包装 `uniform_exception_middleware()`（统一为 `{code,message,data}`）。 |
| `api/auth_router.py` | `router(prefix=/api/auth)`：注册/登录/`me`/改密/管理员用户 CRUD。调用 `core.auth`、`core.rbac`、`core.db`。 |
| `api/chat_router.py` | `router(prefix=/api/chat)`：`POST ""`(非流式)、`POST /stream`(SSE 流式+工具事件)、`GET /history`；`save_chat_history()` 写历史并调 `qa_logger`。 |
| `api/ticket_router.py` | `router(prefix=/api/tickets)`：列表/详情/创建(`pending`)/审批(`pending/approved/rejected/closed`)。 |
| `api/knowledge_router.py` | `router(prefix=/api/knowledge)`：列表/重建/上传(multipart→`datas/`)/删除(删 DB 记录+本地文件+重建，带路径逃逸防护)。 |
| `api/tool_router.py` | `router(prefix=/api/tools)`：`POST /{tool_name}` 按 RBAC 分发到 `business_tools.call_tool()`。 |
| `api/__init__.py` | 包标记。 |

---

## 三、`common/` —— 公共模块

| 文件 | 作用 |
| --- | --- |
| `common/config_base.py` | 通用 env 读取：`get_env()`/`get_env_int()`/`get_env_float()`/`get_env_bool()` 与 `BaseSettings`（APP_ENV/DEBUG/LOG_LEVEL）。被 `core/config.py` 等使用。 |
| `common/constants.py` | 全局常量：`PROJECT_ROOT`、`DATAS_DIR`、`CHROMA_DIR`、`SQLITE_DB_PATH`、`BGE_MODEL_PATH`、`ROLE_PERMISSIONS`、`RAG_SIMILARITY_THRESHOLD`(0.70)、`RAG_TOP_K`、`RATE_LIMIT_*`。被各模块广泛引用。 |
| `common/exception.py` | 异常体系：`ErrorCode`、`AppException` 及 `register_exception_handlers(app)`。**注意：线上未被 `main.py` 实际调用**，实际异常格式由 `api/utils.uniform_exception_middleware` 处理（详见文末附录）。 |
| `common/file_utils.py` | 文件工具：`normalize_path()`（防路径逃逸）、`safe_write_text()`（原子写）、`file_lock()`。**注意：未检索到业务模块调用（疑似预留工具）。** |
| `common/logger_base.py` | 日志工厂：`get_logger()`（TimedRotatingFileHandler，按天滚动）。**注意：仅被 `test_m0_scaffold.py` 探测性 import，运行期未使用。** |
| `common/models/__init__.py` | 定义 `UserRole`/`TicketStatus`/`User`/`Ticket`/`Doc`/`RetrievalResult` 等 Pydantic 模型。**注意：仅 `RetrievalResult` 被 `rag/retriever_pipe.py` 实际使用，其余未被业务采用（遗留/规划层）。** |
| `common/__init__.py` | 包标记。 |

---

## 四、`web/` —— 前端（Streamlit）

| 文件 | 作用 |
| --- | --- |
| `web/app.py` | Streamlit 单入口：`main()` 据 sidebar 切 6 页（登录/对话/对话记录/工单/上传/账号），`render_login/chat/tickets/upload/accounts` 系列；对话页用 `stream_chat` 消费 SSE 并实时渲染 `render_tool_events()`。 |
| `web/frontend_api.py` | 后端 REST/SSE 客户端封装：所有页面 API 调用函数、`parse_sse_events()`、`describe_tool_event()`（工具事件→中文提示）、`PAGE_NAMES`、`api_base_url()`。被 `app.py`、`smoke_test.py` 调用。 |
| `web/local_launcher.py` | Tkinter GUI 启动器：`LocalLauncher` 类提供「检查状态/启动后端/重启后端/注册管理员/启动前端/打开页面/停止服务」按钮，自起 uvicorn+streamlit 并写日志到 `docs/logs/`。被 `.bat` 启动。 |
| `web/smoke_test.py` | M3 前端链路冒烟：直接打真实后端，验证 health/管理员重建索引/员工 SSE 对话/工单列表/管理员导出/上传 .docx。 |
| `web/__init__.py` | 包标记。 |
| `web/*.log`（含 `m3_streamlit.*.log`） | 运行日志文件（Streamlit / M3 冒烟产生的标准输出与错误日志），**仅日志，非源码**。 |
| `docs/logs/backend.log`、`docs/logs/frontend.log` | 由 `local_launcher.py` 写入的后端/前端服务运行日志。 |

---

## 五、`loop_optimizer/` —— 半自动 Loop 优化（M5）

| 文件 | 作用 |
| --- | --- |
| `loop_optimizer/collector.py` | 采集：`load_events()` 读 `agent_server/logs/*.jsonl`，`collect_samples()` 按规则抽不良样本（低匹配/幻觉风险/高频）。 |
| `loop_optimizer/filter.py` | 聚合：`aggregate_samples()` 按归一化问题聚类去重生成 `Finding` 并排序，`_suggestion()` 给优化建议。 |
| `loop_optimizer/models.py` | 数据类：`QAEvent`/`ScoredEvent`/`BadSample`/`Finding`/`OutputArtifacts`。 |
| `loop_optimizer/rules.py` | 评分规则：`normalize_question()`、`max_match_score()`、`rule_risk_score()`（金额/条款/工单号事实命中校验）、`score_event()`、`high_frequency_keys()`。 |
| `loop_optimizer/updater.py` | 产物写出：`write_outputs()` 生成 `bad_sample.csv`/`optimize_report.md`/`prompt_diff.md`（仅人审，不自动改线上）。 |
| `loop_optimizer/run_loop.py` | 入口：`main()` 串起 collect→aggregate→write，受 `LLM_EVAL_ENABLED`（默认关）控制。 |
| `loop_optimizer/__init__.py` | 包标记。 |
| `loop_optimizer/output/` | 产物目录：`bad_sample.csv`、`optimize_report.md`、`prompt_diff.md`（人工审核用，**不会自动修改 `prompt_template.py` 或生产向量库**）。 |

---

## 六、`scripts/` —— 辅助脚本

| 文件 | 作用 |
| --- | --- |
| `scripts/freshman_run.sh` | Linux/CI 引导脚本：校验 `.venv/Scripts/python.exe` 存在 → 跑 `scripts/verify_readme.py` → 打印后端/前端启动命令。 |
| `scripts/verify_readme.py` | README 校验器：`main()` 检查 `README.md` 是否包含必需短语、命令片段、必需路径（如 `docs/*.md`、`loop_optimizer/output/*`、`scripts/freshman_run.sh`）。被 `run_harness.py` 与 `freshman_run.sh` 调用。 |
| `scripts/.gitkeep` | 占位空文件。 |

---

## 七、`docs/` —— 文档与日志

| 文件 | 侧重 |
| --- | --- |
| `docs/architecture.md` | 架构说明：三层解耦 mermaid 图、完整数据流、安全边界（env/临时目录/M5 不自动改 Prompt）。 |
| `docs/api_doc.md` | API 字典：各端点方法、路径、请求/响应示例、统一 JSON 结构、错误码。 |
| `docs/demo_guide.md` | 演示指南：分步本地演示（准备/启动/建管理员/管理员流程/员工问答/Loop 优化）。 |
| `docs/resume_point.md` | 简历量化点：一句话定位、工程亮点、M4 压测结果表、可写入简历的表述。 |
| `docs/logs/` | 启动器写的服务运行日志（`backend.log`/`frontend.log`）。 |
| `docs/.gitkeep` | 占位空文件。 |

---

## 八、`datas/` —— 数据目录（本地）

| 内容 | 作用 |
| --- | --- |
| `datas/chroma/` | Chroma 持久化向量库（`chroma.sqlite3` + 段目录），由 `rag/vector_store.py` 读写。 |
| `datas/app.db`（+`app.db-shm`/`app.db-wal`） | SQLite 生产库，存 user/ticket/chat_history/doc 四表，由 `core/db.py` 读写。 |
| `datas/*.pdf`、`datas/*.docx` | 公司制度文档（8 份示例：差旅管理办法、财务报销制度、人事考勤、员工保密、咨询工单规范、办公用品采购、IDC 运维手册、新员工入职与转正），由 `rag/loader.py` 解析入库。 |

---

## 九、`harness_test/` —— 测试闭环（Pytest + Locust + Allure）

**整体用途**：隔离测试闭环；用临时 `DATAS_DIR/CHROMA_DIR/APP_DB_PATH/QA_LOG_PATH` + `KNOWLEDGE_AGENT_MOCK_LLM=1` 防止污染生产数据，覆盖 M0–M6。

**内部结构**：

| 文件 / 目录 | 作用 |
| --- | --- |
| `harness_test/conftest.py` | 全局 `autouse` fixture，设置隔离环境变量并 `reload` 常量/DB/限流；`api_client` fixture。 |
| `harness_test/fixture/__init__.py`、`fixture/app_client.py` | 测试基础设施，`isolated_client()`（基于 `TestClient` 的隔离客户端）+ `auth_headers()` 注册登录助手。 |
| `test_m0_scaffold.py` | 骨架测试（/health、import、`logger_base` 探测）。 |
| `test_m1_rag.py` | RAG 链路（扫描/解析/OCR/embed/retrieve/阈值）。 |
| `test_m2_agent.py` | 后端 + 鉴权 + RBAC + 对话/工单基础。 |
| `test_m3_account_management.py` | 账号管理（注册/登录/改密/管理员 CRUD）。 |
| `test_m3_chat_ticket_management.py` | 对话与工单流转（SSE/建单/审批）。 |
| `test_m3_web_frontend_api.py` | 前端 API 模块单测（如 `parse_sse_events`）。 |
| `test_langgraph_migration.py` | 验证 `build_graph()` 产出已编译 LangGraph。 |
| `edge/test_m4_api_edges.py` | API 边界（缺 token/重复用户名/越权）。 |
| `edge/test_m4_isolation_contract.py` | 存储路径可覆盖性（隔离契约）。 |
| `func/test_chat_ticket_memory_knowledge_flow.py` | 最近 5 轮记忆 + 知识 + 工单「建议但不自动建单」。 |
| `func/test_m4_functional_flows.py` | M4 功能流（mock retrieve 验证对话/工单/工具）。 |
| `func/test_m5_loop_optimizer.py` | M5 产物生成（写 jsonl→产出 csv/md）。 |
| `func/test_m6_docs.py` | M6 文档校验（`verify_readme` 通过、必需文件存在）。 |
| `stress/locustfile.py` | Locust 压测用户模型（`KnowledgeAgentUser`，注册/对话等并发请求）。 |
| `harness_test/results/` | 测试产物目录：`allure/`（Allure 结果）、`locust_50_*`/`locust_100_*`（Locust 统计/异常/失败 CSV）、`m4_summary.json`（由 `run_harness.py` 生成）、`server/`（uvicorn 日志）。 |
| `harness_test/.tmp/` | pytest 临时目录。 |

---

## 十、`models/` —— 本地模型权重目录

| 子目录 | 作用 |
| --- | --- |
| `models/bge-base-zh-v1.5/` | 本地 Embedding 模型权重，768 维，被 `rag/embed_loader.py` 固定调用，不上云。 |
| `models/qwen2.5-vl/` | 本地 VLM 权重，4-bit 加载，用于插图语义增强（`rag/loader.enhance_image_text`），缺失则回退 PaddleOCR 文本。 |
| `models/bge-reranker-base/` | 本地 reranker 权重，由 `rag/reranker.py` 在 `RERANKER_ENABLED=true` 时启用。 |

> 该目录体积大（模型权重），已被 `.gitignore` 忽略，不入库。

---

## 十一、数据与安全边界（务必知晓）

- `.env` 中密钥只在本机，不进代码、日志、文档、Git。
- 制度文档、`datas/app.db`、`datas/chroma/` 全部本地保存，不上云。
- Embedding、OCR 在本地运行；仅 LLM 生成层走云端 API（key 来自环境变量）。
- 测试与 Loop 优化使用临时目录 / mock LLM，不污染生产 `datas/`。
- Loop 优化只产出建议文件，**不会自动修改线上 Prompt**。

---

## 附录：疑似未使用 / 遗留文件（维护提示）

以下文件在当前代码中**未被业务主链路实际调用**，二次开发或清理时建议核实：

1. `common/exception.py`（`register_exception_handlers`）：线上未被 `main.py` 调用，实际异常格式由 `api/utils.uniform_exception_middleware` 统一处理。
2. `common/logger_base.py`（`get_logger`）：运行期无业务调用，仅 `test_m0_scaffold.py` 探测性 import。
3. `common/file_utils.py`：全局检索未发现有业务模块调用，疑似预留工具。
4. `common/models/__init__.py` 中的 `UserRole`/`TicketStatus`/`User`/`Ticket`/`Doc` 等 Pydantic 模型：仅 `RetrievalResult` 被使用，其余未被业务采用（可能为早期规划遗留）。
5. 文档交叉引用缺失：`项目大纲.md` 提到的 `dev_plan.md`/`codex_prompts.md`，以及 `使用说明书.md` 引用的《学习指南.md》**当前仓库均不存在**，如需配套请另行补全。
