# Knowledge Agent 使用说明书

> 企业内部知识库问答 + 工单智能体（演示 / 验收用）
> 路径：`F:\code\knowledge_agent` ｜ 运行环境：Python 3.12.9（项目虚拟环境 `.venv`）

本说明书面向**使用者**：安装、启动、登录、上传知识库、对话、创建与审批工单、常见排错。
想了解系统原理、代码结构、如何二次开发，请看同级《学习指南.md》。

---

## 0. 一句话说明

这是一个跑在本机的 Web 系统：员工用网页提问公司制度，系统基于本地知识库检索 + 云端大模型生成回答；需要时员工可一键创建工单，管理员在网页上审批。所有制度文档、向量库、数据库都在你本机，**只有"大模型生成回答"这一步调用云端 API**。

---

## 1. 环境准备

### 1.1 确认 Python 版本

全程**只使用项目虚拟环境**的解释器，禁止用系统 `python` / `py`（系统默认是 3.14，与本项目不兼容）。

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe --version
```

期望输出：`Python 3.12.9`。

### 1.2 配置 LLM 密钥（环境变量）

系统不把密钥写进代码。复制模板并填写本机密钥：

```powershell
cd F:\code\knowledge_agent
copy .env.example .env
```

用记事本打开 `.env`，填写（只填你有的那一组即可）：

```ini
# 主用：Agnes（OpenAI 兼容接口）
AGNES_API_KEY=你的密钥
AGNES_BASE_URL=https://apihub.agnes-ai.com/v1
AGNES_MODEL=agnes-2.0-flash

# 备用：Volcano Ark（留空则用上面一组）
# ARK_API_KEY=你的密钥
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# ARK_MODEL=ep-xxxx
```

> ⚠️ 不要把真实 key 写进命令、日志、截图或文档。`.env` 已在 `.gitignore` 中，不会被提交。

### 1.3 数据已就位（一般无需处理）

- 制度文档：`datas/`（PDF / .docx）
- 向量库：`datas/chroma/`
- 数据库：`datas/app.db`
- 本地 Embedding 模型：`models/bge-base-zh-v1.5/`

首次启动后，上传文档会触发索引重建，无需手动准备。

---

## 2. 启动服务

推荐用**本地启动器**（图形界面，一键启停）：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe web/local_launcher.py
```

启动器提供按钮：启动后端 / 启动前端 / 停止后端 / 停止前端 / 重启后端 / 检查状态。

### 手动启动（二选一）

后端（FastAPI）：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m uvicorn agent_server.main:app --host 127.0.0.1 --port 8000
```

前端（Streamlit）：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m streamlit run web/app.py --server.address 0.0.0.0 --server.port 8501 --browser.gatherUsageStats false
```

### 访问地址

| 用途 | 地址 |
| --- | --- |
| 后端健康检查 | http://127.0.0.1:8000/health |
| 后端接口文档（Swagger） | http://127.0.0.1:8000/docs |
| 前端页面 | http://localhost:8501 |

> 健康检查返回 `{"status":"ok"}` 即后端就绪。

---

## 3. 首次登录与账号

系统**不内置固定管理员账号**。首次使用先注册一个管理员：

```powershell
curl -X POST http://127.0.0.1:8000/api/auth/register -H "Content-Type: application/json" -d "{\"username\":\"admin_demo\",\"password\":\"Passw0rd!\",\"role\":\"admin\"}"
```

然后在浏览器打开前端 `http://localhost:8501`，用 `admin_demo / Passw0rd!` 登录。

**管理员创建普通员工**：登录后在"账号"页创建，新建员工默认密码为 `123456`（员工首次登录后可在"账号"页自行修改）。

| 角色 | 能做什么 |
| --- | --- |
| 管理员 admin | 管理知识库（上传/删除/重建）、管理所有账号、查看/审批全部工单、导出工单统计 |
| 员工 employee | 提问对话、查看自己的对话历史与工单、主动创建工单 |

---

## 4. 上传与管理知识库

1. 用**管理员**账号登录前端。
2. 进入"上传"页。
3. 选择 PDF、DOCX 或 DOC 文件，点击"上传并入库"。
4. 系统把文件存入本地 `datas/`，并重建本地 Chroma 索引。

也可在"上传"页的"已入库文档"区域：
- **重建索引**：点击"重建知识库索引"（文档很多或检索异常时用）。
- **删除文档**：勾选目标文档 → 勾选"确认删除" → 点击"删除文档"。删除会移除数据库记录、本地文件，并重建索引。

> 数据边界：文档原文、向量库、SQLite 全在本地；仅 LLM 回答走云端。

---

## 5. 员工对话问答

1. 用**员工**账号登录。
2. 进入"对话"页，输入问题（例如"差旅报销标准是什么"）。
3. 前端会实时展示**工具调用过程**：身份校验 → 知识库检索 → 历史工单匹配 → LLM 决策。
4. 回答生成后，可展开查看引用的知识片段。

对话会读取当前用户**最近 5 条历史问答**作为上下文（短期记忆），仅影响本次回答，不写入长期向量库。

- 查看历史：对话页的"对话记录"或"历史"入口。
- 普通对话**不会自动创建工单**。

---

## 6. 工单流程

### 6.1 员工创建工单

当回答中出现"建议创建工单"时，对话窗口会显示"创建工单"按钮。点击后：
- 调用 `POST /api/tickets`
- 新工单状态为 `pending`（待审批）
- 进入管理员审批队列

员工也可在"工单"页查看自己提交的工单状态。

### 6.2 管理员审批工单

1. 用**管理员**账号登录。
2. 进入"工单"页。
3. 选择工单，将状态改为：
   - `approved`：批准
   - `rejected`：驳回
   - `closed`：关闭

员工只能看自己的工单，不能审批。管理员可看全部工单并导出统计。

---

## 7. API 速查

默认地址 `http://127.0.0.1:8000`。除 `/health`、`/api/auth/register`、`/api/auth/login` 外，均需请求头：

```text
Authorization: Bearer <token>
```

统一响应格式：`{"code":"ok","message":"ok","data":{}}`。

| 分组 | 方法 | 路径 | 说明 |
| --- | --- | --- | --- |
| Health | GET | `/health` | 健康检查 |
| 认证 | POST | `/api/auth/register` | 注册用户 |
| 认证 | POST | `/api/auth/login` | 登录获取 token |
| 认证 | GET | `/api/auth/me` | 当前用户与可用工具 |
| 认证 | POST | `/api/auth/change-password` | 修改密码 |
| 认证 | GET | `/api/auth/admin/users` | 管理员列出用户 |
| 认证 | POST | `/api/auth/admin/users` | 管理员建用户（默认密码 123456） |
| 认证 | POST | `/api/auth/admin/users/{id}/reset-password` | 管理员重置密码 |
| 认证 | DELETE | `/api/auth/admin/users/{id}` | 管理员删除用户 |
| 对话 | POST | `/api/chat` | 非流式问答 |
| 对话 | POST | `/api/chat/stream` | SSE 流式问答 + 工具事件 |
| 对话 | GET | `/api/chat/history` | 当前用户问答历史 |
| 工单 | GET | `/api/tickets` | 工单列表（员工看自己，管理员看全部） |
| 工单 | GET | `/api/tickets/{id}` | 工单详情 |
| 工单 | POST | `/api/tickets` | 用户创建待审批工单 |
| 工单 | PATCH | `/api/tickets/{id}` | 管理员审批/关闭 |
| 知识库 | GET | `/api/knowledge` | 列出已入库文档 |
| 知识库 | POST | `/api/knowledge/upload` | 管理员上传文档（multipart, 字段 `file`） |
| 知识库 | POST | `/api/knowledge/rebuild` | 管理员重建索引 |
| 知识库 | DELETE | `/api/knowledge/{doc_id}` | 管理员删除文档并重建索引 |
| 工具 | POST | `/api/tools/{tool_name}` | 按 RBAC 调用业务工具 |

SSE 事件示例：

```text
event: tool
data: {"tool":"doc_retrieve","count":3}

event: done
data: {"answer":"...","ticket_id":null}
```

---

## 8. 验证与自检命令

```powershell
# RAG 冒烟（加载/重建/检索）
F:\code\knowledge_agent\.venv\Scripts\python.exe -m agent_server.rag.smoke_test

# 后端冒烟（注册→登录→提问→查工单）
F:\code\knowledge_agent\.venv\Scripts\python.exe -m agent_server.smoke_test

# 全量功能测试
F:\code\knowledge_agent\.venv\Scripts\python.exe -m pytest harness_test -q

# M4 压测（50/100 并发短压测）
F:\code\knowledge_agent\.venv\Scripts\python.exe run_harness.py --stress-duration 10s

# M5 Loop 优化报告
F:\code\knowledge_agent\.venv\Scripts\python.exe -m loop_optimizer.run_loop

# README 内容校验
F:\code\knowledge_agent\.venv\Scripts\python.exe scripts\verify_readme.py
```

---

## 9. 关键文件路径

| 用途 | 路径 |
| --- | --- |
| 后端入口 | `agent_server/main.py` |
| 前端入口 | `web/app.py` |
| 本地启动器 | `web/local_launcher.py` |
| 对话路由 | `agent_server/api/chat_router.py` |
| 编排（图） | `agent_server/graph_flow/graph_builder.py` |
| RAG 检索 | `agent_server/rag/retriever_pipe.py` |
| 业务工具 | `agent_server/tools/business_tools.py` |
| 认证/权限 | `agent_server/core/auth.py`、`agent_server/core/rbac.py` |
| 数据库 | `agent_server/core/db.py`（库文件 `datas/app.db`） |
| 全量测试 | `harness_test/` |
| Loop 优化 | `loop_optimizer/run_loop.py` |
| 架构文档 | `docs/architecture.md` |
| API 文档 | `docs/api_doc.md` |
| 演示指南 | `docs/demo_guide.md` |

---

## 10. 常见问题（FAQ）

**登录失败**
确认后端已启动（`/health` 返回 ok），且管理员账号已通过 `/api/auth/register` 注册（系统不预置账号）。

**问答没命中制度内容**
先确认文档已上传并重建索引，再跑 RAG 冒烟定位：
```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m agent_server.rag.smoke_test
```

**工单没有自动创建**
这是设计行为：普通对话不自动落库建单。回答出现"建议创建工单"时，由员工在前端点"创建工单"提交，再经管理员审批。

**删除文档后前端仍有旧结果**
删除会触发索引重建。若前端仍显示旧内容，刷新页面后重新提问。

**改了 `.env` 不生效**
改完需重启后端（在启动器点"重启后端"或停掉 uvicorn 重跑）。

**想关掉限流做压测**
测试脚本已自动关闭；手动调试可设环境变量 `KNOWLEDGE_AGENT_DISABLE_RATE_LIMIT=1`。

---

## 11. 安全与数据边界（务必知晓）

- `.env` 中的密钥只在本机，不进代码、日志、文档、Git。
- 制度文档、`datas/app.db`、`datas/chroma/` 全部本地保存，不上云。
- Embedding、OCR 在本地运行，不调用云端。
- 仅 LLM 生成层走云端 API（key 来自环境变量）。
- 测试与 Loop 优化使用临时目录 / mock LLM，不污染生产 `datas/`。
- Loop 优化只产出建议文件，**不会自动修改线上 Prompt**。

---

_本说明书整合自 `README.md`、`使用说明.md`、`docs/demo_guide.md`、`docs/api_doc.md`、`docs/architecture.md`。更深的原理与代码导读见《学习指南.md》。_
