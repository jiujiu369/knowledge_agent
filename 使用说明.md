# Knowledge Agent 使用说明

本文面向本地演示和验收使用。所有命令均在 `F:\code\knowledge_agent` 下执行，并且必须使用项目虚拟环境：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe
```

不要使用系统 `python` 或 `py`。不要把 `.env` 中的 key 复制到命令、日志或文档中。

## 1. 准备环境

1. 进入项目目录：

```powershell
cd F:\code\knowledge_agent
```

2. 检查 Python 版本：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe --version
```

期望为 Python 3.12.9。

3. 检查环境变量文件：

- `.env.example` 是模板。
- `.env` 是本地真实配置，只保存在本机。
- API key 只放在环境变量里，不写入代码。

## 2. 启动服务

推荐方式：使用本地启动器。

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe web/local_launcher.py
```

启动器可执行：

- 启动后端
- 启动前端
- 停止后端
- 停止前端
- 重启后端
- 检查运行状态

手动启动后端：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m uvicorn agent_server.main:app --host 127.0.0.1 --port 8000
```

手动启动前端：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m streamlit run web/app.py --server.address 0.0.0.0 --server.port 8501 --browser.gatherUsageStats false
```

访问地址：

- 后端健康检查：`http://127.0.0.1:8000/health`
- 后端接口文档：`http://127.0.0.1:8000/docs`
- 前端页面：`http://localhost:8501`

## 3. 首次登录

系统不内置固定管理员账号。首次使用时先注册一个管理员：

```powershell
curl -X POST http://127.0.0.1:8000/api/auth/register -H "Content-Type: application/json" -d "{\"username\":\"admin_demo\",\"password\":\"Passw0rd!\",\"role\":\"admin\"}"
```

然后在前端登录：

- 账号：`admin_demo`
- 密码：`Passw0rd!`

管理员可在“账号”页创建普通用户。普通用户默认密码为 `123456`。

## 4. 上传知识库文档

1. 使用管理员账号登录前端。
2. 进入“上传”页。
3. 选择 PDF、DOCX 或 DOC 文件。
4. 点击“上传并入库”。
5. 系统会将文件保存到本地 `datas/`，并重建本地 Chroma 索引。

数据边界：

- 文档原文保存在本地 `datas/`。
- 向量库保存在本地 `datas/chroma/`。
- SQLite 数据库保存在本地 `datas/app.db`。
- Embedding 和 OCR 均在本机运行。
- 只有 LLM 回答生成会调用云端 API。

## 5. 删除已上传文档

1. 使用管理员账号登录前端。
2. 进入“上传”页。
3. 在“已入库文档”区域选择要删除的文档。
4. 勾选“确认删除所选文档”。
5. 点击“删除文档”。

删除后系统会：

- 删除 `doc` 表中的文档记录。
- 删除 `datas/` 下对应的本地文件。
- 重建知识库索引。

## 6. 用户对话

1. 使用普通用户账号登录。
2. 进入“对话”页。
3. 输入问题。
4. 系统会读取最近 5 条历史问答作为上下文，并结合本地 RAG 检索结果生成回答。

对话窗口会显示：

- 当前登录用户的历史对话。
- 本次提问和回答。
- 工具调用过程。
- 可选的工单创建建议。

普通对话不会自动创建工单。

## 7. 用户创建工单

当回答中出现工单建议时，对话窗口会显示“创建工单”按钮。

用户点击后：

- 系统调用 `POST /api/tickets`。
- 新工单状态为 `pending`。
- 工单进入管理员审批队列。

用户也可以选择“暂不创建”，本次普通对话仍会保存在对话历史中。

## 8. 管理员审批工单

1. 使用管理员账号登录。
2. 进入“工单”页。
3. 选择待处理工单。
4. 将状态改为：

- `approved`：批准
- `rejected`：驳回
- `closed`：关闭

普通用户只能查看自己的工单，不能审批工单。

## 9. 对话记忆说明

当前实现为短期上下文记忆：

- 读取当前用户最近 5 条 `chat_history`。
- 拼入 LLM 上下文。
- 仅影响当前回答生成。
- 不写入长期向量记忆。
- 不把历史数据上传到 Embedding 服务，因为 Embedding 本地运行。

## 10. 常用验证命令

全量测试：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m pytest harness_test -q
```

RAG 冒烟：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m agent_server.rag.smoke_test
```

后端冒烟：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m agent_server.smoke_test
```

M4 测试与短压测：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe run_harness.py --stress-duration 10s
```

M5 Loop 优化：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m loop_optimizer.run_loop
```

README 校验：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe scripts\verify_readme.py
```

## 11. 关键文件路径

- 后端入口：`agent_server/main.py`
- 前端入口：`web/app.py`
- 本地启动器：`web/local_launcher.py`
- RAG 冒烟：`agent_server/rag/smoke_test.py`
- 全量 harness：`harness_test/`
- Loop 优化入口：`loop_optimizer/run_loop.py`
- 架构文档：`docs/architecture.md`
- API 文档：`docs/api_doc.md`
- 演示指南：`docs/demo_guide.md`
- README 校验脚本：`scripts/verify_readme.py`

## 12. 常见问题

### 登录失败

确认后端已启动，并检查账号是否已注册。管理员首次需要通过 `/api/auth/register` 注册。

### 问答没有命中文档

先确认文档已上传并重建索引，再运行：

```powershell
F:\code\knowledge_agent\.venv\Scripts\python.exe -m agent_server.rag.smoke_test
```

### 工单没有自动创建

这是当前设计：普通对话不自动创建工单。用户需要在对话窗口点击“创建工单”，提交后由管理员审批。

### 删除文档后仍有旧结果

删除文档接口会触发索引重建。若前端仍显示旧内容，刷新页面后重新查询。
